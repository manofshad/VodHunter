# Python 3.11 API runtime with the pinned in-process NMFP stack. TensorFlow
# 2.13 requires typing-extensions<4.6, so these API versions are intentionally
# constrained to their compatible Pydantic 1 / SQLAlchemy releases.
-r requirements-nmfp.txt
fastapi==0.103.2
pydantic==1.10.26
uvicorn==0.23.2
python-multipart==0.0.9
python-dotenv==1.0.1
alembic==1.13.1
sqlalchemy==2.0.15
